-- กระดานคะแนนมินิเกมของจริง — รันใน SQL Editor ของ Supabase (รันซ้ำได้)
--
-- เดิมคะแนนของเพื่อนบนกระดานเป็นตัวเลขที่แอปคิดขึ้นเองจากระยะวิ่งสะสม
-- แต่ละเครื่องจึงเห็นไม่ตรงกัน ตอนนี้เก็บคะแนนรายสัปดาห์ไว้บนโปรไฟล์
-- ซึ่งเพื่อนอ่านได้อยู่แล้วตามนโยบาย profiles_select (ไม่ต้องเพิ่มสิทธิ์ใหม่)
--
-- ส่วนตาราง game_scores ยังเป็นความลับของเจ้าของเหมือนเดิม (สถิติสูงสุดรายเกม)

alter table public.profiles
  add column if not exists weekly_score int not null default 0,
  -- เวลาที่อัปเดตคะแนนล่าสุด ใช้ดูว่าคะแนนนั้นเป็นของสัปดาห์นี้หรือสัปดาห์ก่อน
  add column if not exists weekly_score_at timestamptz;
