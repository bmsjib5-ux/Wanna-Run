# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง (Supabase Dashboard → Project Settings → API)
# ถ้าไม่ตั้งค่าสองตัวนี้ แอปจะทำงานแบบเก็บข้อมูลในเครื่องอย่างเดียว
VITE_SUPABASE_URL=https://tozdnimkbimtgwkyxifa.supabase.co
VITE_SUPABASE_ANON_KEY=

# แจ้งเตือนแบบ push บนเว็บ/PWA (สร้างด้วย: node scripts/gen-vapid.mjs)
# ค่านี้เป็นคีย์สาธารณะ เปิดเผยได้ ส่วนคีย์ลับให้เก็บเป็น secret ของ Edge Function
VITE_VAPID_PUBLIC_KEY=
